[uwsgi]
# Increase the buffer size to handle very long OAuth callback URLs and headers.
# The default is 4096, which is often too small for MediaWiki OAuth 2.0 tokens
# combined with Toolforge proxy headers, resulting in "Webservice is unreachable"
buffer-size = 32768
